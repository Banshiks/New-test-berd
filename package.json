{
  "name": "vampire-roguelike",
  "version": "1.0.0",
  "description": "Vampire Survivors-style co-op roguelike for mobile",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "node server/server.js",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0",
    "phaser": "^3.60.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0",
    "vite": "^5.0.0"
  }
}
